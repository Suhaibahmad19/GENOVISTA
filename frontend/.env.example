# Frontend env vars
# Point this to your FastAPI server base URL
# Example if running uvicorn in backend/ at port 8000:
#   uvicorn app.main:app --reload --port 8000
VITE_API_BASE=http://localhost:8000
